import React from "react";
import MainPage from "./Route/MainPage";

const App = () => {
  return (
    <div style={{ fontFamily: "prompt" }}>
      <MainPage />
    </div>
  );
};

export default App;
